# Core runtime
streamlit>=1.20
tensorflow>=2.11,<2.13
keras>=2.11,<2.13
numpy>=1.23
pandas>=1.5
h5py>=3.6

# Optional / dev
jupyterlab>=3.6    # useful for `next-word-predictor.ipynb`
python-dotenv>=0.21

# macOS (Apple Silicon) users: prefer the macOS build (install *instead* of `tensorflow`)
# pip install tensorflow-macos tensorflow-metal
# tensorflow-macos>=2.12

# Install with: pip install -r requirements.txt
